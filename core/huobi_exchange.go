package core

import (
	"math/big"

	"github.com/KyberNetwork/reserve-data/common"
)

var imm_add ethereum.

func sendtoHuobi(token common.Token, amount *big.Int, address Etherium.address) (common.ActivityID, error) {
	tx.err = self.blockchain.Send(token, amount, imm_add)
}
